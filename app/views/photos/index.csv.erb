<%- headers = ['id', 'ptype', 'angle', 'state', 'source', 'description', 'camera', 'rating', 'ftype', "#{@ftype}_url", 'imageabletype', 'imageableid' ] -%>
<%= CSV.generate_line headers -%>
<%- @photos.each do | photo | -%>
<%= CSV.generate_line([
       photo.id,
       photo.ptype,
       photo.view_angle,
       photo.state,
       photo.source,
       photo.description,
       photo.camera,
       photo.rating,
       @ftype,
       image_url(photo.file(@ftype).path),
       photo.imageable_type,
       photo.imageable_id
        ], force_quotes: true
        ).html_safe -%>
<%- end -%>